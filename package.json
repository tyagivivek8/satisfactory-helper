{
  "name": "satisfactory-helper-workspace",
  "private": true,
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "setup": "uv sync --extra dev && pnpm install",
    "dev": "concurrently -k -n api,web -c red,cyan \"powershell -NoProfile -ExecutionPolicy Bypass -File scripts/dev-api.ps1\" \"pnpm --dir apps/web dev\"",
    "start": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/start.ps1",
    "build": "pnpm --dir apps/web build",
    "check": "uv run ruff check apps/server tests && uv run pytest -m \"not integration\" && pnpm --dir apps/web check",
    "test:integration": "uv run pytest -m integration",
    "test:web": "pnpm --dir apps/web test",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "concurrently": "^9.2.1"
  }
}
