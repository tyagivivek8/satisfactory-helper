"""Everything that turns a domain result into something a reader sees.

Presenters may import ``domain`` and ``core``; nothing in those two layers may
import back. ``presenters.text`` is the TSV surface the MCP tools return.
"""
